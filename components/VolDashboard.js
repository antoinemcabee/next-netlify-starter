// import { useAuth } from '../context/auth-context'

const VolunteerDashbaord = () => {

    // const authState = useAuth()

    return (
        <>
            <h1>You are on the volunteer dahsboard</h1>
            {/* <p>Login Type: {authState.loginType}</p> */}
        </>
    )
}

export default VolunteerDashbaord